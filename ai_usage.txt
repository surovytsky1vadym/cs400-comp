AI Usage Log — Practice 3

Tools: Antigravity (Claude Sonnet 4.6), Grok.

Student
- Architecture and decisions: EBNF levels, recursive descent, AST from the handout,
  codegen as a tree walk, precedence/associativity from the grammar, line:column errors.
- First test: tests/valid_p3_basic.txt (2 + 3 * 4).
- Multipass setup and the final run (20/20, result 120).

AI
- Everything after that first test: grammar.ebnf, parser, AST dump, CodeGen,
  the rest of tests/, expected/.ast files.

Constraints kept: no parser generators, no regex, no eval, IR only via llvmlite.ir.
Practice 2 lexer unchanged.